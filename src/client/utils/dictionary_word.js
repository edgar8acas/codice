export default function DictionaryWord({
  dictionaryId,
  word,
  userId,
  isLearned,
}) {
  this.dictionaryId = dictionaryId;
  this.word = word;
  this.userId = userId;
  this.isLearned = isLearned;
}
